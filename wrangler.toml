name = "ngl-foods-backend"
main = "server.ts"
compatibility_date = "2024-04-04"
compatibility_flags = ["nodejs_compat"]

# This tells Cloudflare to serve your React frontend directly, 
# bypassing the Express app's missing file system.
[assets]
directory = "dist"